# xcodegen 生成的工程(源真相是 project.yml,用 `xcodegen generate` 重建)
*.xcodeproj

# 构建产物
/.build/
DerivedData/
*.xcuserstate
xcuserdata/

# 代码覆盖率产物
*.profraw
*.profdata

# SPM
.swiftpm/
.build-spm/

# macOS
.DS_Store

# 分发产物(不提交打包的 DMG)
dist/
*.dmg

# 本地签名覆盖(个人证书/团队,勿提交)
Config/Local.xcconfig

# 设计稿与内部规划(仅本地保留,不同步到仓库)
design/
